# Plan C Linux Training Requirements
# ============================================================================

# Core Dependencies (should already be installed in mmdetection environment)
torch>=1.13.0
torchvision>=0.14.0
mmcv>=2.0.0
mmdet>=3.0.0
mmengine>=0.9.0

# Training Utilities
tensorboard>=2.10.0
yapf>=0.40.1

# Data Processing
numpy>=1.23.0
opencv-python>=4.6.0
Pillow>=9.2.0

# Optional - Better Progress Display
tqdm>=4.64.0
rich>=12.5.0

# Optional - Remote Monitoring
wandb>=0.13.0

# ============================================================================
# Installation Instructions:
# ============================================================================
#
# If mmdetection environment already exists, you only need:
#   pip install tensorboard tqdm rich
#
# For fresh installation on Linux:
#   pip install -r requirements_linux.txt
#
# Verify installation:
#   python -c "import torch, mmcv, mmdet, mmengine; print('All imports OK!')"
